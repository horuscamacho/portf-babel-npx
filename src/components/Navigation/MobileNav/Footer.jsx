export function Footer({children}) {
    return (
        <footer className="h-16 w-full flex justify-center mt-4 animate-fade animate-duration-600 animate-normal">
            {children}
        </footer>
    )

}